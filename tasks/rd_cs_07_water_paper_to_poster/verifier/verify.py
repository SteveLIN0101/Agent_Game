"""Deep grader (family=build) for rd_cs_07_water_paper_to_poster — grades domain correctness vs key."""
from openclaw.reddust.deeplib import verify_for


def verify(world):
    return verify_for(world)
