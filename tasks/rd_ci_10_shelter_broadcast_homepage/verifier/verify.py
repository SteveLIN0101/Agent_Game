"""Deep grader (family=build) for rd_ci_10_shelter_broadcast_homepage — grades domain correctness vs key."""
from openclaw.reddust.deeplib import verify_for


def verify(world):
    return verify_for(world)
