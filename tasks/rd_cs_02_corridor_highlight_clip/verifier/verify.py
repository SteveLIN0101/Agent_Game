"""Deep grader (family=build) for rd_cs_02_corridor_highlight_clip — grades domain correctness vs key."""
from openclaw.reddust.deeplib import verify_for


def verify(world):
    return verify_for(world)
