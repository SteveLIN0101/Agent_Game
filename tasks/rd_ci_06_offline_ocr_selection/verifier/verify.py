"""Deep grader (family=search) for rd_ci_06_offline_ocr_selection — grades domain correctness vs key."""
from openclaw.reddust.deeplib import verify_for


def verify(world):
    return verify_for(world)
