"""Deep grader (family=build) for rd_cs_10_multisize_sos_poster — grades domain correctness vs key."""
from openclaw.reddust.deeplib import verify_for


def verify(world):
    return verify_for(world)
