"""Deep grader (family=build) for rd_ci_11_resident_skill_profile_page — grades domain correctness vs key."""
from openclaw.reddust.deeplib import verify_for


def verify(world):
    return verify_for(world)
