"""Deep grader (family=search) for rd_sr_04_repair_patch_4searches — grades domain correctness vs key."""
from openclaw.reddust.deeplib import verify_for


def verify(world):
    return verify_for(world)
