"""Deep grader (family=build) for rd_ci_01_undocumented_vision_model — grades domain correctness vs key."""
from openclaw.reddust.deeplib import verify_for


def verify(world):
    return verify_for(world)
